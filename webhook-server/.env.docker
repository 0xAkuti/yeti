# Environment variables for Docker Compose
# Copy this file to .env and update with your values

# Blockchain Configuration (REQUIRED)
CONTRACT_ADDRESS=0x1234567890123456789012345678901234567890

# RPC URL Options (choose one based on your setup):
# Option 1: Docker Desktop on Mac/Windows (default)
RPC_URL=http://host.docker.internal:8545

# Option 2: Linux with host networking
# RPC_URL=http://172.17.0.1:8545

# Option 3: Anvil running with --host 0.0.0.0 (recommended)
# RPC_URL=http://host.docker.internal:8545

# Option 4: Use your actual IP address
# RPC_URL=http://192.168.1.100:8545

# Optional: Ngrok authtoken for public webhook URLs
NGROK_AUTHTOKEN=your-ngrok-authtoken-here

# TEE Configuration (automatically set by compose)
# DSTACK_SIMULATOR_ENDPOINT=http://tee-simulator:8090